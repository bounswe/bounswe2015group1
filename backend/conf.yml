database:
    driverClass: org.postgresql.Driver
    user: postgres
    password: pass
    url: jdbc:postgresql://localhost/test

server:
    type: default
    rootPath: /api/*
    applicationConnectors:
        - type: http
          port: 8000
    adminConnectors:
        - type: http
          port: 8001
